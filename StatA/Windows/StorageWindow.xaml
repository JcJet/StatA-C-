<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StatA.Windows"
        xmlns:Custom="http://schemas.stocksharp.com/xaml" x:Class="StatA.Windows.StorageWindow"
        mc:Ignorable="d"
        Title="StorageWindow" Height="240.678" Width="335.594">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>

        <Custom:StorageSettingsPanel />
        <WrapPanel Grid.Row="1">
            <Button Name ="AcceptButon" Content="Применить" />

        </WrapPanel>

    </Grid>
</Window>
